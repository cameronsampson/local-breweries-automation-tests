{
  "name": "local-breweries-automation-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright automation suite for openbrewerydb.org and the OpenBreweryDB API. Verifies the site is live and that the 5 nearest breweries to a configured location match expected country/city within a 40km radius.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:frontend": "playwright test tests/frontend",
    "test:api": "playwright test tests/api",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "install:browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.7.0",
    "dotenv": "^16.4.5",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
